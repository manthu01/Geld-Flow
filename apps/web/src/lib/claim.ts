/** localStorage key a signed-out visitor's claim token is stashed under so the dashboard can pick the trip back up right after sign-in. */
export const PENDING_CLAIM_KEY = "geldflow:pendingClaimToken";
